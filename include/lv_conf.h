/**
 * Project LVGL configuration.
 *
 * LVGL supplies safe defaults for settings not declared here. This small file
 * exists so the library can locate the project configuration during its own
 * compilation. Expand it when DashboardApp starts owning LVGL objects.
 */
#ifndef LV_CONF_H
#define LV_CONF_H

#define LV_COLOR_DEPTH 16
#define LV_MEM_SIZE (64U * 1024U)

#endif  // LV_CONF_H
